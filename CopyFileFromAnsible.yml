---
- name: Copy file playbook
  hosts: all

  tasks:
  
  - name: Copy file
    win_copy:
      src: ./TestFile.txt 
      dest: \\{{ inventory_hostname }}\c$\TestFile.txt
    delegate_to: localhost
