- name: Retrieve credential value
  debug:
    msg: "Username is {{ lookup('tower_credential', 'my_credential username') }} and password is {{ lookup('tower_credential', 'my_credential password') }}"
